import React from 'react';

const Card = () => {
    return (
        <div>
            <img src="#" alt="img"/>
            <div>Random text from API</div>
            <span>Read More</span>
        </div>
    );
};

export default Card;
