import styled from "styled-components";

export const MainTitle = styled.h1`
  color: #0e91cd;
  font-size: clamp(1rem, 50% + 5vw, 3rem);
  font-family: 'Chango', cursive;
  margin-bottom: 30px;
  margin-top: 20px;
  z-index: 1;
`;